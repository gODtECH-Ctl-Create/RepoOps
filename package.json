{
  "name": "repoops",
  "version": "0.2.0",
  "private": true,
  "description": "Open-source IssueOps and repository operations automation for GitHub maintainers.",
  "type": "module",
  "scripts": {
    "test": "node --test",
    "check:scripts": "node --check scripts/simulate.mjs && node --check scripts/sync-labels.mjs && node --check scripts/inspect-linked-prs.mjs",
    "check:site": "node --check site/data.mjs && node --check site/app.mjs",
    "check": "npm run check:scripts && npm run check:site && npm test",
    "simulate": "node scripts/simulate.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
